# 脱敏检查清单（发布前必读）

本仓库计划公开至 GitHub。提交前已做如下扫描与处理：

## ✅ 已确认无泄漏项
- 无 `.env` / `*.pem` / `*.key` / `secrets.json` 等密钥文件（全仓扫描）。
- 未发现真实 API Key、Access Key、Token（如 `sk-...` / `AKIA...` / `Bearer ...`）。
- 未发现内网 IP（如 `192.168.*` / `10.*` / `172.16.*`）；`localhost` / `127.0.0.1` 仅用于本地自检端口，属正常。
- 政策 / 医疗数据为**模拟语料**，文档与代码注释已声明「全部数据均为模拟」，不含真实患者隐私。

## ⚠️ 已处理 / 需注意
- 已移除 `docs/architecture.md` 中对内部规划文档「孙一鸣_发展路线与求职规划.md」的引用（含作者实名与跨工作空间路径），改为中性表述。
- 作品集 / README 中的作者真名「孙一鸣」与实习公司「长江云通」属个人品牌露出，保留有助于背调可信；如需匿名可改为「某信息化交付公司」。

## 📋 Push 前最终确认
- [ ] 确认未包含真实患者数据目录（如 `data/*_real/`，已在 .gitignore 排除）。
- [ ] 确认未提交大模型权重（`*.bin/*.pt/*.gguf/*.safetensors`，已在 .gitignore 排除）。
- [ ] 确认无遗留的本地绝对路径 / 内网地址。
- [ ] 确认 README 横幅与链接位已替换为真实仓库地址。

> 如仓库涉及公司机密或实习未公开项目，请先取得授权再公开。
